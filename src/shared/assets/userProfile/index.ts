import { cdnImage } from '@/shared/lib/cdn';

const userProfileAdd = cdnImage('assets/userProfile/userProfile-add.png');
const userProfileClose = cdnImage('assets/userProfile/userProfile-close.png');
const userProfileDefault = cdnImage('assets/userProfile/userProfile-default.png');
const userProfileEdit = cdnImage('assets/userProfile/userProfile-edit.png');

export const userProfileAssets = {
  add: userProfileAdd,
  close: userProfileClose,
  default: userProfileDefault,
  edit: userProfileEdit,
};
